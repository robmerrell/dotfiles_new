#!/usr/bin/env fish

mkdir -p $HOME/.config
ln -s "$(pwd)/fish" "$HOME/.config/fish"
ln -s "$(pwd)/tmux" "$HOME/.config/tmux"
ln -s "$(pwd)/kak" "$HOME/.config/kak"
ln -s "$(pwd)/helix" "$HOME/.config/helix"
ln -s "$(pwd)/nvim" "$HOME/.config/nvim"
ln -s "$(pwd)/aerospace" "$HOME/.config/aerospace"
ln -s "$(pwd)/wezterm" "$HOME/.config/wezterm"
ln -s "$(pwd)/zellij" "$HOME/.config/zellij"
ln -s "$(pwd)/niri" "$HOME/.config/niri"
ln -s "$(pwd)/noctalia" "$HOME/.config/noctalia"
ln -s "$(pwd)/fuzzel" "$HOME/.config/fuzzel"
